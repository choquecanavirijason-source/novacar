/**
 * Data · Mapper · vehicleMapper
 */

import type { CatalogVehicle, NewCatalogVehicle } from "../../domain/entities/CatalogVehicle";
import type { CatalogVehicleDTO } from "../models/CatalogVehicleDTO";

export const toCatalogVehicle = (dto: CatalogVehicleDTO): CatalogVehicle => ({
  id: dto.id,
  brand: dto.brand,
  model: dto.model,
  year: dto.year,
  price: dto.price,
  tagline: dto.tagline,
  bodyType: dto.body_type as CatalogVehicle["bodyType"],
  fuelType: dto.fuel_type as CatalogVehicle["fuelType"],
  transmission: dto.transmission as CatalogVehicle["transmission"],
  condition: dto.condition as CatalogVehicle["condition"],
  mileageKm: dto.mileage_km,
  horsepower: dto.horsepower,
  seats: dto.seats,
  features: dto.features,
  imageUrl: dto.image_url,
  accentFrom: dto.accent_from,
  accentTo: dto.accent_to,
  highlighted: dto.highlighted,
  color: dto.color,
  doors: dto.doors,
  cylinders: dto.cylinders,
  displacement: dto.displacement,
  driveType: dto.drive_type,
  titleCode: dto.title_code,
  saleDate: dto.sale_date,
  saleTime: dto.sale_time,
  saleLocation: dto.sale_location,
  hasKeys: dto.has_keys,
  damageType: dto.damage_type,
  damageSeverity: dto.damage_severity,
  damageDescription: dto.damage_description,
  notes: dto.notes,
  runAndDrive: dto.run_and_drive,
  highlights: dto.highlights,
});

export const toCatalogVehicles = (dtos: CatalogVehicleDTO[]): CatalogVehicle[] =>
  dtos.map(toCatalogVehicle);

export const toCatalogVehiclePayload = (input: NewCatalogVehicle) => ({
  brand: input.brand,
  model: input.model,
  year: input.year,
  price: input.price,
  tagline: input.tagline,
  body_type: input.bodyType,
  fuel_type: input.fuelType,
  transmission: input.transmission,
  condition: input.condition,
  mileage_km: input.mileageKm,
  horsepower: input.horsepower,
  seats: input.seats,
  features: [...input.features],
  image_url: input.imageUrl,
  accent_from: input.accentFrom,
  accent_to: input.accentTo,
  highlighted: input.highlighted,
  color: input.color,
  doors: input.doors,
  cylinders: input.cylinders,
  displacement: input.displacement,
  drive_type: input.driveType,
  title_code: input.titleCode,
  sale_date: input.saleDate,
  sale_time: input.saleTime,
  sale_location: input.saleLocation,
  has_keys: input.hasKeys,
  damage_type: input.damageType,
  damage_severity: input.damageSeverity,
  damage_description: input.damageDescription,
  notes: input.notes,
  run_and_drive: input.runAndDrive,
  highlights: [...input.highlights],
});